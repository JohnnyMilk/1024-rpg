(()=>{'use strict';
const DIR={left:[0,-1],right:[0,1],up:[-1,0],down:[1,0]};
class V07MoveManager{
 constructor({state,board,units,trace=()=>{}}){this.state=state;this.board=board;this.units=units;this.trace=trace}
 orderedHeroes(dir){const hs=this.state.units.filter(u=>this.units.alive(u)&&u.type==='hero');if(dir==='left')return hs.sort((a,b)=>a.c-b.c);if(dir==='right')return hs.sort((a,b)=>b.c-a.c);if(dir==='up')return hs.sort((a,b)=>a.r-b.r);return hs.sort((a,b)=>b.r-a.r)}
 execute(dir){const delta=DIR[dir];if(!delta)throw new Error('Unknown move direction: '+dir);const [dr,dc]=delta,moved=[],actualMoves=[],merges=[],mergedIds=new Set();for(const hero of this.orderedHeroes(dir)){const start={r:hero.r,c:hero.c};let distance=0,mergedInto=null;while(true){const nr=hero.r+dr,nc=hero.c+dc;if(!this.board.inBounds(nr,nc))break;const target=this.board.at(nr,nc,hero.id);if(!target){hero.r=nr;hero.c=nc;distance++;continue}if(target.type==='hero'&&!mergedIds.has(target.id)&&this.units.canMerge(target,hero)){const sourceId=hero.id;this.units.mergeInto(target,hero);mergedIds.add(target.id);mergedInto=target.id;merges.push({sourceId,targetId:target.id,from:{r:start.r,c:start.c},to:{r:target.r,c:target.c},tier:target.tier,specialPair:!!hero.special&&!!target.special});distance++;break}break}if(distance){const rec={id:hero.id,distance,from:start,to:mergedInto?{r:this.state.units.find(u=>u.id===mergedInto)?.r??hero.r,c:this.state.units.find(u=>u.id===mergedInto)?.c??hero.c}:{r:hero.r,c:hero.c},mergedInto};actualMoves.push(rec);if(!hero.dead)moved.push(rec)}}this.board.compactDead();const tx={dir,changed:actualMoves.length>0||merges.length>0,moved,actualMoves,merges,mergeCount:merges.length};this.trace('MOVE_TRANSACTION',tx);return tx}
}
window.V07MoveManager=V07MoveManager;
})();