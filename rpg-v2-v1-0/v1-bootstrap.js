(function(){'use strict';
const bootError=document.getElementById('bootError');
function fail(msg){if(bootError){bootError.style.display='block';bootError.textContent='V1.0 最終規則載入失敗：'+msg}console.error(msg)}
fetch('../rpg-v2-v0-6/hp-loader.js?v=20260915f').then(r=>{if(!r.ok)throw new Error('hp-loader baseline '+r.status);return r.text()}).then(code=>{
code=code.replace("fetch('game.js?v=20260915c')","fetch('../rpg-v2-v0-6/game.js?v=20260915c')");
const marker="(0,eval)(src+'\\n//# sourceURL=rpg-v2-v0-6/game-v06-runtime.js');";
if(!code.includes(marker))throw new Error('找不到 runtime 注入點');
const patch=`
src=src.replace(/\\['precision','📐','精準','[^']*'\\],?/, '');
src=src.replace(/\\['resonance','💥','共鳴','[^']*'\\]/, "['resonance','💥','波動共鳴','敵人死亡時，以死亡位置為中心向周圍 8 格產生波動傷害。']");
src=src.replace(/\\{id:'encore',icon:'🔁',name:'連奏',desc:'[^']*'\\},?/, '');src=src.replace(/\\{id:'linkage',icon:'🔗',name:'連攜',desc:'[^']*'\\},?/, '');
replaceBlock('function deathTriggerEvents(wave){','function tryResurrect(hero)', \`function deathTriggerEvents(wave){let ev=[];for(let e of wave){if(e.type==='hero'){if(has('deathBlast')){let es=livingEnemies().filter(x=>Math.abs(x.r-e.r)<=1&&Math.abs(x.c-e.c)<=1),lvl=rankLevel('deathBlast'),targets=lvl>=4?es:randomTargets(es,lvl);if(targets.length){for(let x of targets)hitEnemy(x,sdmg(),ev,'deathBlast','💣');useSkill('deathBlast')}}if(has('legacy')){if(!legacy)useSkill('legacy');legacy=true;ev.push({t:'board',icon:'👻 遺志已準備'})}continue}if(has('resonance')&&!e.resonanceNoChain){let lvl=rankLevel('resonance'),es=livingEnemies().filter(x=>Math.abs(x.r-e.r)<=1&&Math.abs(x.c-e.c)<=1),targets=lvl>=3?es:randomTargets(es,lvl);if(targets.length){for(let x of targets){if(lvl<4&&rem(x)<=1)x.resonanceNoChain=true;hitEnemy(x,1,ev,'resonance','💥',e,false)}useSkill('resonance')}}}return ev}\`);
replaceBlock('function mergeEvents(ids,moved){','async function enemyAct(e){', \`function mergeEvents(ids,moved){let ev=[],records=[];for(let mid of ids){let h=E.find(x=>x.id===mid);if(!h)continue;merges++;let v=hv(h);score+=v;mergeScore+=v;let near=livingEnemies().filter(x=>near8(h,x)),slash=randomTargets(near,1);for(let e of slash)hitEnemy(e,1,ev,'mergeMelee','⚔️',h,false);if(merges%3===0){let arrows=randomTargets(livingEnemies(),2);for(let e of arrows)hitEnemy(e,1,ev,'mergeArrow','🏹',h,true)}if(legacy){let lvl=rankLevel('legacy'),es=livingEnemies(),targets=lvl>=4?es:randomTargets(es,lvl);for(let e of targets)hitEnemy(e,1,ev,'legacy','👻');legacy=false}if(v>best){best=v;RPGSave.recordBestHero(v);records.push(v)}}if(has('overload')&&ids.length>=2){let lvl=rankLevel('overload'),d=lvl>=4?ids.length:lvl,targets=livingEnemies();for(let e of targets)hitEnemy(e,d,ev,'overload','🔥');if(lvl>=4){for(let h of livingHeroes()){h.guard=true;h.guardBy=null;ev.push({t:'buff',e:h,text:'🛡️ 守護'})}}if(targets.length||lvl>=4)useSkill('overload')}if(has('wind')){let lvl=rankLevel('wind'),threshold=lvl>=3?3:4;if(moved>=threshold){let es=livingEnemies(),targets=lvl>=4?es:randomTargets(es,lvl===1?1:2);for(let e of targets)hitEnemy(e,1,ev,'wind','🌪️');if(targets.length)useSkill('wind')}}return{ev,records}}\`);
src=src.replace("祭司實際移動後，自己與周圍 8 格內友方獲得守護；下一次受到傷害時傷害 -1，之後消耗。","祭司實際移動後，自己獲得守護，並從周圍 8 格內友方隨機選擇最多 3 名獲得守護；優先選擇目前沒有守護的友方。下一次受到傷害時傷害 -1，之後消耗。");
const oldGuard="else if(h.profession==='priest'){for(let ally of livingHeroes().filter(x=>Math.abs(x.r-h.r)<=1&&Math.abs(x.c-h.c)<=1)){ally.guard=true;ally.guardBy=h.id;ev.push({t:'buff',e:ally,text:'🛡️ -1'})}}",newGuard="else if(h.profession==='priest'){let nearby=livingHeroes().filter(x=>x.id!==h.id&&near8(h,x)),fresh=nearby.filter(x=>!x.guard),guarded=nearby.filter(x=>x.guard),targets=randomTargets(fresh,Math.min(3,fresh.length));if(targets.length<3)targets.push(...randomTargets(guarded,Math.min(3-targets.length,guarded.length)));let grant=ally=>{let was=!!ally.guard;ally.guard=true;ally.guardBy=h.id;if(!was)ev.push({t:'buff',e:ally,text:'🛡️ 守護'})};grant(h);for(let ally of targets)grant(ally)}";if(!src.includes(oldGuard))throw new Error('找不到祭司守護基礎技能片段');src=src.replace(oldGuard,newGuard);
src=src.replace("const N=4,BI=20,MM=TIMING.MOVE,GM=TIMING.MERGE,FX=TIMING.EFFECT,DM=TIMING.DEATH;","let ROWS=4;const N=4,COLS=4,BI=20,MM=TIMING.MOVE,GM=TIMING.MERGE,FX=TIMING.EFFECT,DM=TIMING.DEATH;");
src=src.replace("for(let i=0;i<N*N;i++){let d=document.createElement('div');d.className='cell';grid.appendChild(d)}","let bossCollapseStage=0,bossWarningRow=-1,bossCollapseFresh=false;function rebuildGrid(){grid.innerHTML='';board.style.setProperty('--v07-rows',ROWS);board.classList.toggle('v07-expanded',ROWS>4);for(let r=0;r<ROWS;r++)for(let c=0;c<COLS;c++){let d=document.createElement('div');d.className='cell'+(r===bossWarningRow?' v07-warning':'');d.dataset.r=r;d.dataset.c=c;grid.appendChild(d)}}rebuildGrid();");
src=src.replace("function empty(){return emptyCells(E,N)}","function empty(){let a=[];for(let r=0;r<ROWS;r++)for(let c=0;c<COLS;c++)if(!E.some(e=>rem(e)>0&&e.r===r&&e.c===c))a.push([r,c]);return a}");
src=src.replace("for(let k=1;k<N;k++){let r=source.r+dr*k,c=source.c+dc*k;if(r<0||r>=N||c<0||c>=N)break;","for(let k=1;k<Math.max(ROWS,COLS);k++){let r=source.r+dr*k,c=source.c+dc*k;if(r<0||r>=ROWS||c<0||c>=COLS)break;");
src=src.replaceAll("nr<0||nr>=N||nc<0||nc>=N","nr<0||nr>=ROWS||nc<0||nc>=COLS");
src=src.replaceAll("nr>=0&&nr<N&&nc>=0&&nc<N","nr>=0&&nr<ROWS&&nc>=0&&nc<COLS");
const bossPrefix="let b=addEnemy(true);if(b){bactive=true;render();spawnFx(b);";
const bossPrefixNew="let shift=E.filter(u=>rem(u)>0).map(u=>rec(u,u.r,u.c,u.r+2,u.c));ROWS=6;bossCollapseStage=0;bossWarningRow=-1;bossCollapseFresh=false;rebuildGrid();anim.play('skill-tag',{text:'⚠️ BOSS APPROACH · 戰場擴張'});await wait(460);for(let u of E)u.r+=2;if(shift.length)await animateMoves(shift);render();let bp=randomItem(empty().filter(x=>x[0]<2))||randomItem(empty());let b=null;if(bp){b={id:id++,type:'boss',r:bp[0],c:bp[1],hits:0,maxHits:bmax(),damage:bdmg()};E.push(b)}if(b){bactive=true;render();spawnFx(b);anim.play('skill-tag',{text:'👑 巨象守門者 · 戰場擴張 4×6'});";
if(!src.includes(bossPrefix))throw new Error('找不到 Boss 建立片段');src=src.replace(bossPrefix,bossPrefixNew);
src=src.replace("bactive=false;next=turn+BI;bossKills++","bactive=false;next=turn+BI;bossKills++;bossCollapseStage=1;bossCollapseFresh=true;bossWarningRow=ROWS-1;rebuildGrid();anim.play('skill-tag',{text:'⚠️ 戰場崩塌預警 · 裂痕格仍可使用'})");
const collapseFn=\`async function v07CollapseStep(){if(!bossCollapseStage)return;let row=bossWarningRow,cells=[...grid.children].filter(x=>+x.dataset.r===row);cells.forEach(x=>x.classList.add('v07-collapsing'));anim.play('skill-tag',{text:'⬇️ ENEMY PHASE · 警告格崩塌'});await wait(700);let doomed=E.filter(x=>x.r===row&&rem(x)>0);for(let e of doomed){e.hits=max(e);e.noReviveFromCollapse=true}if(doomed.length)await settle([]);E=E.filter(x=>x.r!==row);ROWS--;bossWarningRow=-1;if(ROWS>4){bossCollapseStage++;bossCollapseFresh=true;bossWarningRow=ROWS-1;rebuildGrid();render();anim.play('skill-tag',{text:'⚠️ 下一排龜裂 · 還有一次 Player Phase'})}else{bossCollapseStage=0;bossCollapseFresh=false;rebuildGrid();render();anim.play('skill-tag',{text:'✓ 戰場恢復 4×4'})}}\`;
src=src.replace("function tryResurrect(hero){if(!hero||hero.type!=='hero'||hero.revived||!hasClass('priestRevive'))return null;","function tryResurrect(hero){if(!hero||hero.noReviveFromCollapse||hero.type!=='hero'||hero.revived||!hasClass('priestRevive'))return null;");src=src.replace("async function move(d){",collapseFn+"\\nasync function move(d){");
src=src.replace("turn++;render();await enemyPhase();if(!livingHeroes().length){check();busy=false;return}await postTurn();","turn++;render();if(bossCollapseStage&&!bossCollapseFresh){await v07CollapseStep()}else{if(bossCollapseFresh)bossCollapseFresh=false;await enemyPhase()}if(!livingHeroes().length){check();busy=false;return}await postTurn();");
src=src.replace("function reset(){E=[];turn=0;","function reset(){ROWS=4;bossCollapseStage=0;bossWarningRow=-1;bossCollapseFresh=false;rebuildGrid();E=[];turn=0;");
`;
code=code.replace(marker,patch+'\n'+marker.replace('rpg-v2-v0-6/game-v06-runtime.js','rpg-v2-v1-0/game-v1-runtime.js'));
const blob=new Blob([code],{type:'text/javascript'}),url=URL.createObjectURL(blob),s=document.createElement('script');s.src=url;s.onload=()=>URL.revokeObjectURL(url);s.onerror=()=>fail('runtime script 啟動失敗');document.head.appendChild(s);
}).catch(e=>fail(e.message));
})();